"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.GitHubPublisher = void 0;
const child_process_1 = require("child_process");
const crypto_1 = require("crypto");
const fs = __importStar(require("fs"));
class GitHubPublisher {
    publish(app, decisions) {
        const safeProjectId = this._safeToken(app.projectId);
        const branch = `ddd/${safeProjectId}`;
        try {
            (0, child_process_1.execFileSync)('git', ['checkout', '-b', branch], { stdio: 'pipe', shell: false });
            // Write spec and decisions JSON
            fs.writeFileSync('ddd-spec.json', JSON.stringify(app.spec, null, 2));
            fs.writeFileSync('ddd-decisions.json', JSON.stringify(decisions, null, 2));
            (0, child_process_1.execFileSync)('git', ['add', 'ddd-spec.json', 'ddd-decisions.json'], { stdio: 'pipe', shell: false });
            (0, child_process_1.execFileSync)('git', ['commit', '-m', 'chore: DDD generated app'], { stdio: 'pipe', shell: false });
            (0, child_process_1.execFileSync)('git', ['push', '-u', 'origin', branch], { stdio: 'pipe', shell: false });
            const titleName = typeof app.spec['name'] === 'string' ? app.spec['name'] : app.projectId;
            const prUrl = (0, child_process_1.execFileSync)('gh', [
                'pr',
                'create',
                '--title',
                `DDD: ${titleName}`,
                '--body',
                'Generated by Dopamine Driven Development',
                '--head',
                branch,
            ], { stdio: 'pipe', shell: false }).toString().trim();
            return {
                repositoryUrl: this._repoUrl(),
                branchName: branch,
                pullRequestUrl: prUrl,
            };
        }
        catch (err) {
            // GitHub step failed — return partial result so local state is preserved
            return { repositoryUrl: '', branchName: branch, pullRequestUrl: '' };
        }
    }
    _repoUrl() {
        try {
            return (0, child_process_1.execFileSync)('gh', ['repo', 'view', '--json', 'url', '-q', '.url'], { stdio: 'pipe', shell: false })
                .toString()
                .trim();
        }
        catch {
            return '';
        }
    }
    _safeToken(value) {
        const sanitized = value.replace(/[^A-Za-z0-9_-]/g, '-');
        if (sanitized.length > 0) {
            return sanitized;
        }
        const suffix = (0, crypto_1.createHash)('sha1').update(value).digest('hex').slice(0, 8);
        return `token-${suffix}`;
    }
}
exports.GitHubPublisher = GitHubPublisher;
//# sourceMappingURL=githubPublisher.js.map